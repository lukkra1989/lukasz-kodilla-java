package com.kodilla.good.patterns.challenges.food2door;

public class OrderGenerator {
    public boolean createOrder(){
//        Order order= new Order(createOrder().getOrderName(),createOrder().getOrderAmmount());
        return  true;
    }
}
